# 3DSpider repository prompts

This directory is the standard home for 3DSpider-specific Markdown prompts.
AutoDev's generic `run-prompt.yml` can load a prompt here with
`prompt_scope: target` and a path under `.agents/prompts/`.